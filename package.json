{
  "name": "whatsfest-temp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Redirecting to frontend build' && cd frontend && npm run build",
    "install": "echo 'Installing frontend deps' && cd frontend && npm install"
  }
}